# Backend files moved to root for easier deployment

When deploying to Render/Railway, the following structure is used:
- Root directory contains backend code
- Frontend is in `client/` folder

This makes deployment configuration simpler.
